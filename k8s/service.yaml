apiVersion: v1
kind: Service
metadata:
  name: blockid-api
  namespace: blockid
  labels:
    app: blockid-api
spec:
  type: ClusterIP
  selector:
    app: blockid-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
