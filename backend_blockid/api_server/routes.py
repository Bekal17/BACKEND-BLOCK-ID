"""
API route definitions — REST endpoints.

Responsibilities:
- Define routes for health check, get trust score by wallet, list wallets,
  and any admin or internal endpoints.
- Validate request params and delegate to service/database layer.
- Return consistent JSON responses and HTTP status codes.
"""
