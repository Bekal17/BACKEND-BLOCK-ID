"""
HTTP middleware — logging, auth, rate limiting.

Responsibilities:
- Request/response logging, correlation IDs, timing.
- Optional API key or JWT validation.
- Rate limiting per client or per endpoint.
"""
