"""
FastAPI/ASGI application entrypoint.

Responsibilities:
- Build and configure the ASGI app (FastAPI or similar).
- Mount routers for health, trust scores, and wallet endpoints.
- Wire middleware (CORS, logging, auth) and exception handlers.
"""
