"""
Environment variable loading and validation helpers.

Responsibilities:
- Parse and coerce env vars (e.g., ports as int, booleans).
- Optional .env file support (e.g., python-dotenv).
- Centralize env names to avoid typos and duplication.
"""
